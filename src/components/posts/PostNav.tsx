const PostNavbar = (): JSX.Component => {
    return (
        <header class="navbar">
            <nav>
                <ul role="list">
                    <li>
                        <a href="/">← Regresar al inicio</a>
                    </li>
                </ul>
            </nav>
        </header>
    );
};

export default PostNavbar;
